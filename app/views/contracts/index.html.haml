%h1 Listing contracts

%table
  %tr
    %th Person
    %th Post
    %th Film
    %th
    %th
    %th

  - @contracts.each do |contract|
    %tr
      %td= contract.person_id
      %td= contract.post_id
      %td= contract.film_id
      %td= link_to 'Show', contract
      %td= link_to 'Edit', edit_contract_path(contract)
      %td= link_to 'Destroy', contract, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Contract', new_contract_path
